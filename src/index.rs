//! 索引管理模块

/// 索引管理器
pub struct IndexManager {
    // TODO: 添加字段
}

impl IndexManager {
    /// 创建新的索引管理器
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for IndexManager {
    fn default() -> Self {
        Self::new()
    }
}
